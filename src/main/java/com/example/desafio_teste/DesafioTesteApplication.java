package com.example.desafio_teste;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Classe main que executa o código por completo.
 */
@SpringBootApplication
public class DesafioTesteApplication {

	/**
	 * Classe main
	 *
	 * @param args chams todos os argumentos do projeto
	 */
	public static void main(String[] args) {
		SpringApplication.run(DesafioTesteApplication.class, args);
	}

}
